const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); supabase.from('quizzes').select('*').order('created_at', { ascending: false }).limit(1).then(({data}) => { const id = data[0].id; console.log('LATEST_ID:', id); const puppeteer = require('puppeteer'); (async () => { const b = await puppeteer.launch(); const p = await b.newPage(); p.on('console', m=>console.log('LOG:', m.text())); await p.goto('http://localhost:3001/q/' + id); await new Promise(r=>setTimeout(r, 5000)); await p.screenshot({path: 'final_test.png'}); await b.close(); process.exit(0); })(); });
